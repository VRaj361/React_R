import React from 'react'

export const ContactUs = (ele) => {
    console.log(ele.ele)
    //it will work as json to fetching the element we use to write in parameter {ele} because this turm is in simple var
  return (
    <div>ContactUs</div>
  )
}
